make it all rake-Tasks
  - export (options: no-comments)

- TODOs
- check if column data types from browser are possible (varchar-lengths)
- index-page
- move files into subdirectory to clean up the uppermost level
- secure .close calls by ensure
